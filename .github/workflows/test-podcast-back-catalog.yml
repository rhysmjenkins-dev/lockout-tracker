name: Test podcast voice style

on:
  push:
    branches:
      - agent/podcast-back-catalog-test
    paths:
      - .github/workflows/test-podcast-back-catalog.yml
      - scripts/podcast/generate-draft.mjs
      - scripts/podcast/build-back-catalog-review.mjs

permissions:
  contents: write
  pull-requests: write

concurrency:
  group: lockout-podcast-voice-style-test
  cancel-in-progress: false

jobs:
  generate-tests:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
      LOCKOUT_API_URL: https://script.google.com/macros/s/AKfycbxwKK71BSc8n4qxxyZVMGrHa7ycBYCG7qddBPeGgbysb_io5PANaP_HVZ2hppewjbiM-A/exec
      PODCAST_TEST_MODE: 'true'
      PODCAST_TEST_BRANCH: automation/podcast-voice-style-review
      PODCAST_VOICE_STYLE: dry-pundit

    steps:
      - name: Check out the isolated test branch
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 24
          package-manager-cache: false

      - name: Install audio converter
        run: |
          sudo apt-get update
          sudo apt-get install --yes ffmpeg

      - name: Generate latest historical episode with the experimental voices
        run: PODCAST_START_DATE="2026-08-10" PODCAST_END_DATE="2026-08-16" node scripts/podcast/generate-draft.mjs

      - name: Build one review page
        run: node scripts/podcast/build-back-catalog-review.mjs

      - name: Commit test outputs to a non-live branch
        env:
          OUTPUT_BRANCH: ${{ env.PODCAST_TEST_BRANCH }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git checkout -B "$OUTPUT_BRANCH"
          git add podcasts/test-output
          git commit -m "Generate podcast voice-style test"
          git push --force origin "HEAD:refs/heads/$OUTPUT_BRANCH"

      - name: Open or update the isolated review pull request
        env:
          GH_TOKEN: ${{ github.token }}
          OUTPUT_BRANCH: ${{ env.PODCAST_TEST_BRANCH }}
        run: |
          if gh pr view "$OUTPUT_BRANCH" --json number --jq .number >/dev/null 2>&1; then
            gh pr edit "$OUTPUT_BRANCH" --base "$GITHUB_REF_NAME" --title "Podcast voice-style comparison" --body-file podcasts/test-output/README.md
          else
            gh pr create --draft --base "$GITHUB_REF_NAME" --head "$OUTPUT_BRANCH" --title "Podcast voice-style comparison" --body-file podcasts/test-output/README.md
          fi

      - name: Keep a downloadable copy
        uses: actions/upload-artifact@v6
        with:
          name: lockout-podcast-voice-style-test
          path: podcasts/test-output
          retention-days: 14
